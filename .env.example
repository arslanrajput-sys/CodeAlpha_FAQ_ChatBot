# Add the real value only in Vercel Environment Variables. Never commit it.
GEMINI_API_KEY=your_gemini_api_key_here
# Optional override; the backend defaults to gemini-2.5-flash-lite.
# GEMINI_MODEL=gemini-2.5-flash-lite
